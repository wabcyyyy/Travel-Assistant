"""自然语言入口域：把用户的一句话变成结构化操作或对话回复。

阶梯位置：**最上层**（``generation`` 之上）——只准 import 下层域，不被任何域 import。

- ``chat_draft/``：把"改行程"的自然语言请求产出可确认的安全草稿
  （intent 意图解析 → validate 安全校验 → document 投影补水 → plan_edit 执行层 →
  hotel 酒店子系统 → decide 编排与对外入口）；
- ``local_replan``：受约束的局部重规划；
- ``nl_edit``：自然语言增量编辑解析（一句话 → 结构化操作，不全量重生成）；
- ``clarify``：意图确认节点（多轮对话收集行程条件与偏好）；
- ``city_guide``：城市引导对话（省份/模糊输入自动推荐省域内热门城市）。

核心范式：**LLM 当规划者产出结构化意图，确定性代码当执行者落地并校验**，最终由用户确认
后才落库（写路径一律走 ``app/services/``）。

不设 re-export 面：域内符号按子模块路径导入。
"""
