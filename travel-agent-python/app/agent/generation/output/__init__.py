"""生成链路的输出落地层：把已排好的行程装配成最终产物，并做终检与时间窗优化。

- ``assembly``：``format_output`` 编排（各阶段串联）；
- ``facts``：候选快照命中后的权威字段回填与溯源标注；
- ``prices``：酒店/餐饮定价链（联网实时价 → 基准价×季节系数）与预算重算；
- ``quality``：成品行程的终检、降级原因汇总与质量结论；
- ``critic``：可解释的软质量评审（quality 用它）；
- ``schedule_optimizer``：确定性行程时间窗优化器。

**位置说明（别照抄蓝图的"下游"字面）**：本层在编排层**之下**——是编排层（``graph_nodes`` /
``workflow``）调用本层把行程落地。蓝图里说的"formatting 是 workflow 的下游"指**执行顺序**
（生成完之后才装配），不是 import 方向；import 方向永远是上层调下层。
"""
