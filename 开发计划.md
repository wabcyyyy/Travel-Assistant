### 调整说明

> ✅ **交付状态（2026-08-19）**：阶段 0-4 全部完成。git 提交：`3160c4f`（阶段0+1 三端骨架与主链路）、`66d1e5a`（阶段2 地图+编辑+预算）、`91660f5`（阶段3 RAG+反思+Redis+PDF导出）、`cad8abd`（阶段4 测试体系）、`24d8396`（高德/千问真实密钥联调+LLM真实评测）、`（待提交）`（性能压测）。
>
> 关键实测数据：LLM 真实链路评测（qwen-plus，30 用例×2遍）工具调用准确率 100%、时间冲突率 0%、幻觉检出率 3.33%、两遍一致性 33.3%（20 用例不稳定，报告含清单）；核心接口压测（20 workers）540-744 QPS、p50 11-15ms、错误率 0%。高德 POI/地理编码真实联调通过（修复 RestTemplate 二次编码）；前端地图渲染需在控制台给 JS key 配置 `localhost:5173` 域名白名单后浏览器实测。

已按「实习求职 + vibecoding 直接生成代码」的目标做了精简：

1. 移除非核心重组件：去掉 CI/CD、RocketMQ、Nacos、混沌测试等过度工程化内容，用轻量方案替代；
2. 格式改为**分阶段 + 分模块 + 明确代码单元**，可直接逐段投喂给 vibecoding 生成代码；
3. 保留三大求职核心亮点：双栈架构设计、LangGraph Agent 能力、AI 专项测试体系，复杂度控制在实习项目的合理深度。

**第二轮修订（评审意见落实）**：补齐版本锁定、预算单一口径、地图职责划分、知识库规模、密钥管理、测试指标公式与可复现性、已知坑位预案，并重新平衡阶段 3 工作量。

---

## 一、项目基础信息

### 1. 核心交付目标

实现一个可演示、有代码深度、贴合后端/测开/Agent 岗面试的智能旅行助手，核心功能：

- 智能行程生成（Agent 驱动）
- 地图可视化打点与路线绘制
- 预算自动计算与明细展示
- 行程增删改编辑，实时同步
- PDF/图片导出

### 2. 技术栈与版本锁定

> ⚠️ 硬性约定：**版本必须在生成代码前一次性锁定**（pom.xml / pyproject.toml / package.json 固定版本号），投喂时按本表指定，禁止让生成器自行选择大版本，否则极易产出不可运行代码。

| 层级 | 选型 | 版本要求 | 兼容性注意点 |
| --- | --- | --- | --- |
| 业务后端 | Java 17 + Spring Boot 3.2.x | 3.2.x | — |
|  | MyBatis-Plus | 3.5.5+ | 必须用 `mybatis-plus-spring-boot3-starter`（普通 starter 不兼容 Spring Boot 3） |
|  | JJWT | 0.12.x | 0.11.x 与 0.12.x API 完全不同，生成时明确 0.12 |
|  | Redis | spring-boot-starter-data-redis | — |
| AI Agent 服务 | Python 3.12 + FastAPI | 最新稳定版 | — |
|  | LangGraph / LangChain | 同大版本锁定 | langchain 已拆包：`langchain-core`、`langchain-community`、`langchain-openai`；豆包/通义用官方 SDK（dashscope / volcengine）自写兼容层，不依赖 langchain 内置集成 |
|  | uv | 最新稳定版 | pyproject.toml 锁定全部依赖 |
| 前端 | Vue 3.4+ + Vite 5+ + TS | 最新稳定版 | — |
|  | Element Plus | 2.7+ | — |
|  | 高德地图 | JS API 2.0 | 需两个 key：Web 服务 key（后端）+ JS key（前端，控制台配域名白名单） |
| 存储 | MySQL 8.0 / Redis 7.x | — | — |
|  | Chroma | **持久化模式** PersistentClient | 用持久化而非内存模式，避免重启重建索引；数据目录 `data/` 入 .gitignore |
| 第三方 | 豆包 / 通义千问 API | — | key 一律走环境变量，不硬编码、不入库、不进 git |
| 测试体系 | JUnit 5 + Mockito / pytest + httpx | — | 见阶段 4 指标定义 |

### 3. 统一目录结构约定（生成代码时直接套用）

```
travel-agent-project/
├── travel-backend-java/       # Spring Boot 业务后端
├── travel-agent-python/       # FastAPI Agent 服务
├── travel-frontend-vue/       # Vue3 前端
├── sql/                       # 建表脚本与初始化数据
└── data/                      # Chroma 持久化数据目录（gitignore）
```

---

## 二、关键设计决策（生成代码前先看，避免返工）

| # | 决策 | 内容 |
| --- | --- | --- |
| D1 | **预算单一口径** | 单价数据源唯一：`poi_knowledge`（门票）+ 城市消费系数表（餐饮/交通/酒店）。Agent 生成时仅做**粗估**（随结果展示）；所有落库与编辑后重算**只走 Java 预算引擎**。Python 侧不维护独立费用逻辑，只读同一份系数数据，保证前后台数字一致。 |
| D2 | **地图路线职责划分** | 前端用高德 JS API 的 Driving 插件直接绘制路线（不走后端）；Java 后端高德封装**只保留地理编码 + POI 搜索**，去掉路径规划，避免双端路线不一致、双倍消耗 API 配额。 |
| D3 | **知识库规模** | `poi_knowledge` 种子数据 **100+ 条**（5 个热门城市 × 20+ 景点），字段含门票、游玩时长、开放时间、经纬度、标签（亲子/网红/人文）。10 条撑不起 RAG 语义检索，也做不了幻觉检测对照库；用脚本批量生成 + 人工抽验。 |
| D4 | **密钥管理** | Java：`application.yml` 用 `${ENV_VAR}` 占位；Python：`.env`（gitignore）；前端 JS key 在控制台配**域名白名单**。所有 key 不入库。 |
| D5 | **接口契约先行** | Python 服务启动后导出 `openapi.json` 并提交仓库，Java/前端据此对齐字段。任何新增/改动字段先改契约再写代码。 |
| D6 | **已知坑位预案** | ① OpenPDF + Thymeleaf 导出 PDF 中文乱码：需引入中文字体文件（如思源黑体）并注册到 PDF 库；② 导出图片用 html2canvas 对高德地图 canvas 会跨域白屏：改为基于行程数据渲染**静态卡片图**（仅非地图 DOM 区域），不加 html2canvas 依赖。 |

---

## 三、分阶段代码生成计划

### 阶段 0：工程骨架生成（第 1 周前 3 天）

**目标**：三个服务一键启动，打通双栈 HTTP 调用，数据库连接正常。骨架工作量小，**剩余时间提前完成阶段 1 的接口契约约定（D5）**。

#### 1. Java 后端骨架生成

投喂指令参考：生成 Spring Boot 3.2 项目骨架（MyBatis-Plus 3.5.5+ / JJWT 0.12），包含以下内容

- 依赖：Spring Web、mybatis-plus-spring-boot3-starter、MySQL Driver、Redis、Lombok、Spring Security、JJWT 0.12、Validation
- 分层目录：`controller / service / serviceImpl / mapper / entity / vo / dto / config / common`
- 基础能力：
  - 统一返回结果类 `Result<T>`、全局异常处理器、跨域配置
  - MyBatis-Plus 配置、分页插件
  - Redis 配置、RestTemplate 配置
  - Spring Security + JWT 基础鉴权配置（放行测试接口）
- 测试接口：`/api/test/hello`、`/api/test/call-agent`（调用 Python 服务测试连通性）
- 密钥：`application.yml` 中所有 key 用 `${ENV_VAR}` 占位（D4）

#### 2. Python Agent 骨架生成

投喂指令参考：生成 FastAPI 项目骨架，使用 uv 管理依赖（pyproject.toml 锁定版本）

- 依赖：fastapi、uvicorn、langgraph、langchain-core、langchain-openai、dashscope（或 volcengine 官方 SDK）、pydantic、httpx、chromadb
- 目录结构：```
travel-agent-python/
├── app/
│   ├── api/          # 接口层
│   ├── agent/        # Agent 工作流、节点、工具
│   ├── common/       # 配置、常量、大模型客户端封装
│   └── schemas/      # 请求响应结构体
└── main.py
```
- 基础能力：
  - 大模型客户端单例封装（支持豆包/通义千问，读 `.env`）
  - 测试接口：`/api/agent/hello`、`/api/agent/test-generate`（返回模拟行程数据）
  - 统一响应格式，和 Java 侧约定对齐；启动后导出 `openapi.json` 提交仓库（D5）

#### 3. 前端骨架生成

投喂指令参考：生成 Vue 3 + TS + Vite 项目骨架

- 依赖：element-plus、axios、pinia、vue-router、@amap/amap-jsapi-loader
- 目录结构：`views / components / router / store / api / utils`
- 基础页面：首页、行程生成页、行程详情页、我的行程页
- 基础能力：
  - Axios 封装（请求/响应拦截、Token 携带）
  - 高德地图加载封装，基础地图组件（可渲染地图）
  - Element Plus 全局引入，基础布局
- 高德 JS key 在控制台配置域名白名单（D4）

#### 4. 数据库初始化

投喂指令参考：生成 MySQL 建表 SQL

- 核心表：`sys_user`、`itinerary_main`、`itinerary_day`、`itinerary_item`、`budget_detail`、`poi_knowledge`、`city_consumption`（城市消费系数表，D1 的数据来源）
- 初始化种子数据：**poi_knowledge 100+ 条**（5 个热门城市 × 20+ 景点，含门票/时长/开放时间/经纬度/标签），city_consumption 覆盖对应 5 城市（D3）

#### 本阶段交付

三个服务均可独立启动，Java 可正常调用 Python 测试接口，数据库可正常读写。

---

### 阶段 1：核心主链路闭环（第 2 周）

**目标**：跑通「用户登录→提交需求→Agent 生成行程→数据入库→前端展示」完整流程。

#### 1. Java 后端代码生成

按模块生成：

1. **用户模块**
   - 实体类、Mapper、Service、Controller
   - 注册、登录、获取用户信息接口
   - 密码加密、JWT 生成校验
2. **行程管理模块**
   - 行程主表、日行程、行程项、预算明细的实体类与 Mapper
   - Service 层：创建行程、查询行程列表、查询行程详情、删除行程
   - Controller 层：对应 RESTful 接口，加鉴权注解
3. **Agent 调用封装**
   - DTO：行程生成请求体、响应体结构体（对齐 openapi.json，D5）
   - `AgentService`：封装 HTTP 调用 Python 服务的逻辑，含超时处理、异常兜底
   - 生成行程业务逻辑：调用 Agent → 解析结果 → 拆解入库 → 返回前端

#### 2. Python Agent 代码生成

按模块生成：

1. **数据结构定义**：行程生成请求、标准化行程输出结构体（pydantic 强校验，直接生成 openapi.json）
2. **基础工具集**：
   - 景点检索工具：读取 poi_knowledge，按城市返回景点列表
   - 景点信息补全工具：返回门票、时长、地址、标签等信息
3. **LangGraph 工作流**（StateGraph，**预留条件边**）
   - 需求解析节点：提取目的地、天数、偏好、预算
   - 景点检索节点：调用工具获取候选景点
   - 行程生成节点：大模型按天生成结构化行程
   - 格式化输出节点：强制输出标准 JSON
   - **预留**：生成节点后挂一条「通过/需修正」条件边（阶段 3 直接挂反思节点，只改图配置不重写结构）
4. **接口层**：`/api/agent/v1/generate` 接口，接收参数启动工作流，返回结果

#### 3. 前端代码生成

1. 登录注册页面
2. 行程生成表单页：目的地、起止日期、人数、偏好、预算
3. 行程详情页：按天展示景点、餐饮、酒店、时间安排
4. 我的行程列表页
5. 接口联调：对接登录、生成、查询、删除接口

#### 本阶段交付

完整可演示的最小闭环：用户登录后提交需求，等待几秒即可看到生成好的行程。

---

### 阶段 2：地图可视化 + 行程编辑 + 预算计算（第 3 周）

**目标**：完成产品核心体验，是项目演示的主要亮点。

#### 1. Java 后端代码生成

1. **高德地图 API 封装模块**（遵循 D2，不做路径规划）
   - 地理编码：地址转经纬度
   - POI 搜索：关键字搜索景点/餐饮（供行程项添加）
2. **预算计算引擎**（唯一口径，D1）
   - 按门票、餐饮、酒店、交通分类计算，数据源 = poi_knowledge + city_consumption
   - 生成结构化预算明细，存入 `budget_detail` 表
   - 编辑后重算、Agent 生成入库时也统一走本引擎校准
3. **行程编辑接口**
   - 新增/删除单日行程项
   - 调整行程项顺序
   - 修改行程项信息（时长、费用、备注）
   - 编辑后自动重算预算（Java 引擎），前端同步刷新路线

#### 2. Python Agent 代码生成

1. 就近排序逻辑：基于经纬度欧氏距离排序景点，减少绕路（**不调高德路径规划**，省配额、逻辑可单测）
2. 费用粗估工具：读取 city_consumption 同一份系数数据做粗估，仅供生成结果展示（D1）
3. 新增增量调整接口：`/api/agent/v1/adjust`，支持针对单个景点做替换推荐

#### 3. 前端代码生成

1. **地图组件核心能力**
   - 批量标注景点 Marker，点击弹出信息窗
   - 按天用 **Driving 插件**绘制游览路线（D2）
   - 侧边栏行程项与地图 Marker 双向联动高亮
2. **行程编辑交互**
   - 拖拽调整景点顺序
   - 点击删除、搜索添加景点
   - 编辑后实时刷新地图与预算
3. **预算看板组件**
   - 总预算展示、分类占比饼图
   - 每日费用明细列表

#### 本阶段交付

地图可交互、行程可编辑、预算实时同步，核心功能完整。

---

### 阶段 3：Agent 增强 + 导出功能 + 轻量工程化（第 4 周）

**目标**：提升项目深度，补充面试亮点，功能完全体。

#### 1. Java 后端代码生成

1. **Redis 缓存模块**
   - 缓存热门城市 POI 数据，减少高德 API 调用
   - 缓存行程详情，提升访问速度
   - 注解式缓存，配置过期策略
2. **异步导出功能**（用 `@Async` 替代消息队列，轻量且能体现异步思想）
   - PDF 生成：Thymeleaf 模板 + OpenPDF，**注册中文字体**（D6①）
   - 导出任务记录：生成任务 ID，支持轮询查询状态
   - 下载接口：导出完成后返回下载链接
3. **第三方 API 降级**：高德/大模型调用失败时，返回兜底数据，避免报错
4. **接口限流**（可选增强，剩余时间不够可砍）：基于 Redis + 自定义注解，限制单用户生成次数

#### 2. Python Agent 代码生成

1. **轻量 RAG 检索**
   - Chroma 持久化模式向量库，启动时自动导入 poi_knowledge（100+ 条，D3）
   - 景点检索节点接入语义检索，支持偏好匹配（亲子、网红、人文等），检索不到时回退关键词检索
2. **反思校验节点**（LangGraph 条件分支，挂在阶段 1 预留的条件边上）
   - 校验维度：时间冲突、开放时间匹配、单日行程饱和度
   - 不通过则返回生成节点修正，最多循环 2 次
   - 校验日志随结果返回，便于调试
3. Prompt 优化：加入输出格式强约束（JSON Schema 提示），减少解析失败

#### 3. 前端代码生成

1. 导出功能：导出 PDF（调后端）、导出图片（**静态卡片图方案，D6②**）两个入口
2. 加载状态优化：生成中、导出中进度提示
3. 异常提示：接口失败、生成失败的友好提示

#### 本阶段交付

具备 RAG + 反思闭环的 Agent、完整导出能力、基础工程化容错设计。

---

### 阶段 4：测试体系建设（测开方向核心亮点，第 5 周）

**目标**：体现测试开发思维，做差异化竞争，不做过度冗余。

#### 1. 后端单元测试

- Java 侧：JUnit 5 + Mockito，覆盖 Service 层核心逻辑（预算计算、行程编辑）
- Python 侧：pytest，覆盖工具函数、节点逻辑、数据格式化、就近排序逻辑

#### 2. 接口自动化测试

- 统一用 pytest + httpx 编写，覆盖用户、行程、导出三大模块核心接口
- 包含正常流、异常流、参数校验、权限校验用例
- **测试数据管理**：独立测试库 `travel_test`，pytest fixture 负责用例前后置清库、token 与测试数据准备，保证用例可重复执行
- 输出可执行的测试脚本与测试报告

#### 3. AI Agent 专项测试（求职核心亮点，必须做）

> **可复现性约定**：评测全部使用固定模型 + temperature=0 + 固定 prompt 版本，必要时用本地规则 MockLLM 兜底；同一批 30 条用例连跑两遍，一致性差则标记为不稳定用例，保证指标可复现、可展示。

生成自动化评测脚本，包含：

1. **工具调用准确率测试**
   - 30 条标准测试用例，覆盖不同城市/天数/偏好
   - 统计：工具调用准确率 = 正确调用次数 / 总调用次数；参数错误率 = 参数错误调用次数 / 总调用次数；结果使用率 = 工具结果被最终行程引用的次数 / 工具调用次数
2. **行程质量量化评估**（指标公式先约定，按公式实现打分）
   - 时间冲突率 = 同日相邻行程项中时间重叠的对数 / 同日相邻行程项总对数
   - 景点重复率 = 重复出现的景点次数 / 全部景点出现次数
   - 预算偏差率 = |估算预算 − 参考预算| / 参考预算（参考预算 = 单价与城市系数汇总）
   - 批量运行后输出评分报告
3. **幻觉检测脚本**（依赖 100+ 条知识库，D3）
   - 比对生成结果与 poi_knowledge：识别虚假景点（不在库中/城市不符）、错误价格（与库中参考价偏差超阈值）
   - 幻觉检出率 = 检出的虚假景点数 / 输出景点总数，输出统计报告

#### 4. 性能基础测试（可选）

- 用 JMeter 对核心查询接口做简单压测，输出 QPS 与响应时间数据
- 统计大模型生成的首 Token 延迟、总耗时

#### 本阶段交付

完整的测试用例集 + 自动化评测脚本 + 量化测试报告（指标含公式说明），面试可直接展示。

---

## 四、vibecoding 使用建议

1. **先锁版本再生成**：开工前按「技术栈与版本锁定」表把三个服务的依赖版本一次性定死，投喂时显式带上版本号；
2. **契约先行**：Python 侧先出 `openapi.json`，Java/前端按契约对齐字段，任何字段变更先改契约（D5），减少联调返工；
3. **逐阶段生成**：不要一次性投喂全部需求，按阶段 0→4 顺序来，每个阶段内再分模块生成，保证代码质量；
4. **生成即验证**：每个小模块生成后立刻运行验证，再进入下一个模块，避免最后集中排错。

需要我先把**阶段 0 的全套骨架代码 + 建表 SQL（含 100+ 条种子数据生成脚本）** 直接生成出来吗？你可以直接导入运行，跳过基础搭建。
