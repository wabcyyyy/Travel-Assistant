# Travel-Assistant 智能旅行规划助手 — 项目介绍

> 本文以当前工作树为准，定位为可运行的求职作品/本地演示系统，不等同于已经上线的生产系统。文中“当前实现”与“后续规划”分开描述；如果项目以团队形式完成，面试时还应根据实际情况补充个人负责模块。

---

## 一、项目定位与技术全景

### 1.1 做了什么

一个**端到端的 AI 旅行规划系统**：用户输入目的地、天数、人数和偏好，Agent 自动规划每日行程（景点→餐饮→酒店），生成带时间轴、地图路线、预算明细的完整方案，并支持自然语言对话式二次编辑。

**核心功能清单**：

| 功能 | 说明 |
|------|------|
| 行程生成 | 输入城市/天数/人数/偏好 → AI 生成每日景点+餐饮+酒店+时间轴 |
| 行程边界 | 单次新建、对话扩展和草稿确认统一最多 7 天，超限请求直接拒绝 |
| 对话编辑 | 用自然语言修改已生成的行程（删景点、换酒店、改时间、加天数等） |
| 酒店推荐 | 按档次筛选候选酒店，展示房型/价格/季节系数，支持一键替换 |
| 城市引导 | 不确定去哪？AI 对话帮你锁定目的地 |
| 槽位澄清 | 信息不全时自动追问（目的地/天数/人数），前端表单联动 |
| 地图可视化 | 高德地图 SDK 展示每日路线、景点坐标、酒店位置 |
| 预算面板 | 按门票/餐饮/交通/酒店分类汇总，支持预算上限预警 |
| PDF 导出 | 异步导出行程为 PDF，支持轮询下载 |
| 管家讲解 | 行程完成后 AI 生成整体规划思路讲解 |
| 景点介绍 | 批量为每个景点生成详细介绍文本 |

### 1.2 技术栈总览

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **前端** | Vue 3 + TypeScript + Vite + Element Plus | SPA 单页应用 |
| | 高德地图 JS SDK | POI 检索、路线规划、地图展示 |
| | ECharts | 预算分类饼图 |
| | vuedraggable | 行程项拖拽排序 |
| | Pinia | 状态管理（用户登录态） |
| **后端** | Spring Boot 3 + MyBatis-Plus | Java 业务层 |
| | MySQL 8 | 主数据库（11 张表） |
| | Redis | Spring Cache 缓存行程详情 |
| | JWT | 无状态鉴权（24h 过期） |
| **Agent** | Python FastAPI | Agent HTTP 服务 |
| | LangGraph | 工作流编排（StateGraph） |
| | ChromaDB | 持久化检索索引（RAG 召回层） |
| | 通义千问 qwen-plus/turbo | qwen-plus 负责行程生成；qwen-turbo 负责对话决策；对话编辑启用 JSON mode，首版生成依靠 Prompt Schema + 校验 |
| **外部 API** | 高德地图 Web API / 官方 MCP（可选） | POI 检索、坐标落地；MCP 失败时兼容 Web API |
| | Unsplash / Wikipedia | 景点图片获取 |
| | DashScope enable_search | 可选的酒店当前挂牌参考价查询，不代表指定日期实时成交价 |

### 1.3 架构拓扑

```
用户浏览器
  ↕ HTTP/REST (JWT Bearer Token)
Vue3 前端 (Vite dev / Nginx)
  ↕ HTTP/REST
Java Spring Boot (:8080)
  ├── MySQL (行程/对话/预算/知识库/用户偏好)
  ├── Redis (行程详情缓存 @Cacheable)
  ↕ HTTP/REST (X-Agent-Token 内部鉴权)
Python FastAPI Agent (:8000)
  ├── LangGraph (完整生成图 + 逐日生成图)
  ├── ChromaDB (RAG 向量检索)
  ├── LLM Client (通义千问: qwen-plus + qwen-turbo)
  └── 外部 API (高德/Unsplash/Wikipedia)
```

**数据流**：前端 → Java 后端（建单/异步编排）→ Python Agent 的 `generate-day` 逐日生成图（生成→反思→重试→fallback）→ Java 落库、预算重算和清缓存 → 前端轮询展示渐进结果。Python 仍保留 `/v1/generate` 的完整生成图（parse→search→generate→reflect→format），用于直接调用和离线评测。对话编辑走 `chatTurn` 闭环：Java 组装上下文 → Python Agent 做动作决策 → Java 校验/用户确认/事务落库。

### 1.4 项目目录结构

```
Travel-Assistant/
├── travel-frontend-vue/          # Vue3 前端
│   └── src/
│       ├── views/
│       │   ├── HomeView.vue          # 首页（Hero CTA）
│       │   ├── GenerateView.vue      # 行程生成表单 + AI 对话澄清
│       │   ├── TripsView.vue         # 行程列表（可点击跳转）
│       │   ├── TripDetailView.vue    # 行程详情 + 对话编辑 + 地图 + 预算
│       │   ├── LoginView.vue         # 登录注册
│       │   └── ExportView.vue        # PDF 导出
│       ├── api/index.ts              # API 封装层
│       ├── router/index.ts           # 路由 + 守卫
│       ├── styles/theme.css          # 全局主题（赤陶橙暖沙风格）
│       └── types/itinerary.ts        # TypeScript 类型定义
│
├── travel-backend-java/          # Spring Boot 后端
│   └── src/main/java/com/travel/backend/
│       ├── controller/
│       │   ├── ItineraryController.java    # 行程 CRUD + 对话编辑端点
│       │   ├── AuthController.java         # 登录注册
│       │   ├── ExportController.java       # PDF 导出
│       │   ├── AmapController.java         # 地图/POI/图片接口
│       │   └── TestController.java         # 本机联通性检查
│       ├── serviceImpl/
│       │   ├── ItineraryServiceImpl.java       # 对外业务门面，保持原有接口契约
│       │   ├── ItineraryGenerationService.java # 建单、校验天数、启动异步生成
│       │   ├── ItineraryCommandService.java    # 行程 CRUD、排序和直接编辑
│       │   ├── ItineraryQueryService.java      # 列表、详情、权限和 VO 组装
│       │   ├── ItineraryChatService.java       # 对话、历史、草稿版本和待确认动作
│       │   ├── ItineraryPlanApplyService.java  # 草稿/酒店方案校验与事务应用
│       │   ├── ItineraryCityService.java       # 城市引导、澄清和支持城市查询
│       │   ├── UserPreferenceService.java      # 用户偏好统计与 top 偏好查询
│       │   ├── ItineraryAsyncPlanner.java      # 逐日异步生成编排器
│       │   ├── ItineraryStatusRecovery.java    # 启动和定时恢复中断的生成任务
│       │   └── AgentServiceImpl.java           # Python Agent 调用层
│       ├── entity/                         # MyBatis-Plus 实体（11 个）
│       ├── mapper/                         # Mapper 接口
│       ├── dto/                            # 请求/响应 DTO
│       ├── vo/                             # 视图对象
│       └── common/
│           ├── SecurityUtils.java          # JWT 工具
│           └── BizException.java           # 业务异常
│
├── travel-agent-python/          # Python Agent
│   └── app/
│       ├── agent/
│       │   ├── workflow.py             # LangGraph 主图（生成→校验→修复循环）
│       │   ├── generators.py           # LLM 生成 + 确定性兜底双路线
│       │   ├── reflect.py              # 规则校验器（时间/营业覆盖/路线/饱和度）
│       │   ├── trace.py                # 脱敏节点/工具/LLM 运行轨迹
│       │   ├── observability.py        # 运行指标聚合与失败摘要
│       │   ├── tools.py                # 检索工具集（RAG/高德/图片）
│       │   ├── clarify.py              # 多轮槽位收集
│       │   ├── day_stream.py           # 分天流式生成
│       │   ├── geo.py                  # 球面距离 + 最近邻排序
│       │   ├── pricing.py              # 联网实时房价查询
│       │   ├── butler.py               # AI 管家讲解
│       │   ├── city_guide.py           # 城市引导对话
│       │   └── chat_draft/             # 对话编辑子系统
│       │       ├── decide.py           # 编排层（封闭动作集路由）
│       │       ├── validate.py         # 结构校验 + JSON 解析修复
│       │       ├── document.py         # 行程文档序列化
│       │       ├── plan_edit.py        # 补丁应用 + 确定性缩短/延长
│       │       ├── intent.py           # 意图识别（缩减/扩展/模糊浏览）
│       │       └── hotel.py            # 酒店候选确认流程
│       ├── rag/
│       │   ├── store.py                # ChromaDB 持久化向量库
│       │   └── embeddings.py           # 自研轻量 n-gram 哈希 embedding
│       ├── common/
│       │   ├── llm_client.py           # LLM 网关客户端（httpx）
│       │   └── config.py               # 配置管理
│       └── schemas/trip.py             # Pydantic 数据模型
│
└── sql/                          # 数据库脚本
    ├── schema.sql                # 主 schema（11 张表）
    ├── seed_data.sql             # 基准种子数据（125 条，5 个城市）
    └── add_*.sql                 # 增量迁移
```

当前完整知识库快照在执行杭州、酒店等增量脚本后约为 186 条 POI，覆盖北京、上海、杭州、成都、西安、三亚 6 个城市；该数量按 `seed_data.sql`、`add_hangzhou.sql`、`add_hotels.sql` 和 `add_hangzhou_hotel_options.sql` 的当前脚本内容估算，实际以数据库中已执行的脚本为准。

行程天数统一限制为 1～7 天：新建请求由 Java/Python 请求模型校验，对话式增减天数和 Java 侧草稿确认也会再次校验，超过 7 天不会触发生成或落库。

### 1.5 主要接口与用户链路

| 链路 | 关键接口 | 说明 |
|------|---------|------|
| 认证 | `POST /api/auth/register`、`POST /api/auth/login` | 注册、登录并获取 JWT |
| 生成 | `POST /api/itinerary/generate` | Java 建单后异步逐日调用 Agent |
| 渐进查看 | `GET /api/itinerary/{id}` | 前端轮询生成状态和已完成日期 |
| 澄清/引导 | `POST /api/itinerary/clarify`、`POST /api/itinerary/city-guide` | 收集槽位、辅助确定目的地 |
| 对话编辑 | `POST /api/itinerary/{id}/chat-edit`、`POST /api/itinerary/{id}/apply-plans` | 生成草稿，用户确认后事务应用 |
| 酒店确认 | `POST /api/itinerary/{id}/hotel-option` | 选择酒店和房型后应用住宿变更 |
| 导出 | `POST /api/export/pdf/{id}`、任务查询和下载接口 | 异步生成 PDF |

Agent 内部接口包括 `/v1/generate`、`/v1/generate-day`、`/v1/chat-turn`、`/v1/clarify`、`/v1/metrics` 和 `/v1/runs/{run_id}`；除健康检查外，配置 `AGENT_INTERNAL_TOKEN` 后由 `X-Agent-Token` 保护生成和编辑接口。

---

## 二、核心架构设计亮点

### 2.1 "LLM 当规划者，代码当执行者"的分离范式

这是整个项目最核心的架构决策。Agent 不使用 OpenAI Function Calling / Tool Calling 协议——LLM **只负责结构化意图输出**（JSON），所有写操作由确定性代码执行并校验。

**为什么不直接用 Function Calling？**

旅行规划是**强正确性**场景——时间冲突、地理不合理、价格虚高都会直接损害用户体验。让 Agent 自主决定工具调用顺序会导致不可控的规划路径。我们选择让 LLM 做它最擅长的事（结构化决策），用确定性代码做它不擅长的事（精确执行、事务写入、版本控制）。

**具体体现**：

**1. 行程生成链路**（`generators.py:137-198`）

LLM 只输出结构化 JSON（`daily_plans` + `budget_estimate`），景点/餐饮/酒店全部从候选列表中选择，价格取候选原值：

```python
# generators.py:150-156 — System Prompt 中的反幻觉约束
"景点和酒店必须从候选列表中选择，不得编造。"
"每个行程项的 cost 必须直接取候选数据中的 ticket_price 字段原值"
"严禁自行编造价格。"
```

首版生成的 API 调用目前通过 `Prompt Schema` 要求结构化 JSON，再经过解析、字段补水、确定性校验和 fallback；并非所有生成请求都启用了 API 层 `response_format=json_object`。对话编辑链路才显式启用 JSON mode，见 `chat_draft/decide.py`。

**2. 对话编辑子系统**（`chat_draft/decide.py:46-87`）

封闭动作集把任意自然语言归约到 5 种有限决策：

| 动作 | 场景 | 输出 |
|------|------|------|
| `hotel_proposal` | 涉及住宿/酒店变更 | 酒店请求（从候选列表选） |
| `plan_update` | 小修（删/加/移动/改时间） | `patches` 补丁列表 |
| `rewrite_plan` | 大改（改天数/整体重排） | 完整 `plan_document` |
| `clarify` | 信息不足/冲突 | 澄清问题 |
| `no_change` | 无需修改 | 空操作 |

```python
# decide.py:51 — 动作集定义
"1) hotel_proposal：凡涉及住宿、酒店、宾馆、房型或某酒店品牌，无论措辞，都必须用它；"
"2) plan_update：对现有行程'小修小补'——移动单个项目、改时间、删/加个别景点"
"3) rewrite_plan：对行程做'大改/重生成'，例如改变总天数"
"4) clarify：信息不足、存在冲突或无法安全安排时使用"
"5) no_change：确实无需修改时使用"
```

**3. Java 侧的校验层**（`ItineraryPlanApplyService.applyPlans`）

从 AI 消息的 `plansJson`（权威数据源）读取、逐项校验结构合法性、事务内逐条落库，防止模型"偷偷改数据"。

**面试价值**：这体现了对 LLM 局限性的深刻理解——模型擅长"想"但不擅长"做"，与其依赖不稳定的 tool calling 协议，不如让模型做它最擅长的事（结构化决策），用确定性代码做它不擅长的事（精确执行、事务写入、版本控制）。

### 2.2 异步渐进式生成 + 伪流式可见

行程生成不是一次性返回，而是**逐日增量生成**，解决了真实的产品问题：生成较长行程时，用户可以先看到已完成日期，而不是面对整段请求的空白 loading。当前项目没有对“首日固定 3 秒”做专项基准测试，因此这里不把 30 秒到 3 秒作为硬性性能指标。

**完整流程**（`ItineraryAsyncPlanner.java:50-94`）：

```java
@Async
public void planDays(Long userId, Long itineraryId, GenerateRequest request, JsonNode context) {
    // 1. 等待壳数据可见（最多重试 10 次，每次 500ms）
    for (int i = 0; i < 10; i++) {
        if (mainMapper.selectById(itineraryId) != null) break;
        Thread.sleep(500);
    }
    List<String> usedNames = new ArrayList<>();  // 跨日去重状态
    String chosenHotel = null;                    // 跨日酒店状态

    for (int dayNo = 1; dayNo <= request.getDays(); dayNo++) {
        // 2. 构建 payload（含 used_names/chosen_hotel/context）
        payload.put("used_names", usedNames);
        payload.put("chosen_hotel", chosenHotel);
        payload.put("context", context);

        // 3. 调用 Python Agent 生成单日
        JsonNode node = agentService.generateDay(payload);

        // 4. 落库 + 更新跨日状态
        persistDay(itineraryId, request, dayNo, plan);
        plan.getItems().forEach(i -> usedNames.add(i.getPoiName()));
        evictCache();  // 清缓存，前端轮询可见新数据
    }
    finish(userId, itineraryId, request);  // 5. 设 status=2 + 生成管家讲解/景点介绍
}
```

**关键设计决策**：

1. **建单壳先行**（`ItineraryGenerationService.generate`）：立即建主表 status=1（生成中）+ 预创建 N 个空日，**不加 @Transactional**——异步线程必须在壳数据提交后才启动，否则查不到单
2. **跨日状态管理**：Java 侧维护 `usedNames`（已选景点名集合）和 `chosenHotel`（已选酒店名），逐日回传给 Python，确保整条行程的连贯性（不重复选同一景点、住同一家酒店）
3. **每天完成即清缓存**：`evictCache()` 确保前端轮询能看到最新数据
4. **可选增强不阻塞**：管家讲解和景点介绍在 `finish()` 中作为可选增强执行，失败只 warn 不影响 status=2（完成状态）

**前端渐进展示效果**：用户可以看到“第1天已完成，第2天生成中……”的 loading 状态；该设计减少一次性等待的空白感，但实际耗时仍受 LLM、网络和候选数据规模影响。

### 2.3 多层降级容错体系

这是项目工程化程度最高的部分，形成了"五层防御纵深"：

| 层级 | 机制 | 代码位置 | 说明 |
|------|------|---------|------|
| 1 | **宽松 JSON 解析** | `generators.py:295-304` | 剥 markdown 代码围栏 + 首尾大括号截取 |
| 2 | **self-repair 重试** | `decide.py:110-124` | 发专门修复请求："只输出语法正确的JSON" |
| 3 | **反馈回喂重试** | `workflow.py:88-89` / `decide.py:183-201` | 校验问题作为 feedback 注入 prompt |
| 4 | **确定性兜底算法** | `generators.py:96-133` | 最近邻排线 + 时间槽模板填充 |
| 5 | **签名校验** | `decide.py:218-225` | `_hotel_signature` 检测模型篡改住宿数据 |

**第 1 层：宽松 JSON 解析**（`generators.py:295-304`）

```python
def _parse_json(raw: str) -> dict:
    text = raw.strip()
    if text.startswith("```"):
        text = text.split("\n", 1)[-1]
        text = text.rsplit("```", 1)[0]
    start = text.find("{")
    end = text.rfind("}")
    if start == -1 or end == -1:
        raise ValueError("LLM 输出中未找到 JSON")
    return json.loads(text[start : end + 1])
```

**第 2 层：self-repair 重试**（`decide.py:110-124`）

```python
try:
    return _parse_json_object(raw)
except DecisionJsonError:
    repaired = client.chat(
        [
            {"role": "system", "content": "修复下面的JSON。保持原意，只输出一个语法正确的JSON对象，不要解释。"},
            {"role": "user", "content": raw[:12000]},
        ],
        temperature=0,
        model=settings.llm_fast_model or None,
        json_mode=True,
    )
    return _parse_json_object(repaired)
```

**第 3 层：反馈回喂**（`decide.py:183-201`）

模型提案未通过确定性校验时，把校验要点作为 feedback 让模型再修一轮：

```python
feedback = (
    "你上一轮返回的 plan_update 未通过校验，不要修改。请严格只使用现有行程中真实存在的 item_id"
    "（切勿编造 id）；缩短行程时必须把被移除日期里的全部项目用 delete 删除或 move 到保留日期；"
    "hotel 项目不得出现在 patches 中；也不要改动价格、坐标、城市、人数或预算。"
)
repaired = _decide_plan_change(req, hotels, feedback=feedback)
```

**第 4 层：确定性兜底算法**（`generators.py:96-133`）

LLM 不可用时完全不依赖 LLM，用最近邻算法 + 时间槽模板生成：

```python
def fallback_generate(city, days, persons, preferences, hotels=None, ...):
    ordered = nearest_neighbor_order(attractions)  # haversine 球面距离最近邻
    for day_no in range(1, days + 1):
        for slot_index in range(_daily_attraction_target(days)):
            poi = ordered[cursor % len(ordered)]
            start, end = TIME_SLOTS[slot_index]  # 09:00-11:30, 13:30-16:00, 19:00-20:30
            items.append(_to_item(poi, start, end))
        # 餐饮 + 酒店
```

**第 5 层：签名校验**（`decide.py:218-225`）

```python
if _hotel_signature(plans) != _hotel_signature(req.plans):
    if not (len(plans) != req.days):
        return ChatTurnResponse(
            reply="### 需要先确认住宿\n\n检测到酒店发生变化。请选择酒店和房型后再应用",
            plans=[], changed=False, ...)
```

**面试价值**：这是区分"demo"和"产品"的关键——真实场景中 LLM 一定会失败，关键是你在失败时能做什么。五层降级体现了对系统可靠性的系统性思考。

### 2.4 高德官方 MCP 的标准化接入

Agent 侧不再把高德 REST API 直接散落在业务代码中，而是通过 `app/integrations/amap_mcp.py` 使用高德官方 MCP Server 的 Streamable HTTP 接入方式。当前项目实际接入的是只读 POI 搜索能力，并保留详情/地理编码的工具别名适配；天气、公交和路径规划尚未进入 Agent 主链路。

```text
LangGraph 节点
    ↓
amap_mcp（工具发现、调用超时、结果解析）
    ↓
高德官方 MCP Server
    ↓
统一 POI 领域对象（source/source_fetched_at）
```

当前接入策略是“启用时官方 MCP 优先、旧 Web API 兼容降级、本地知识库保底”：

1. MCP 返回的 POI 与本地知识库合并；同名 POI 保留本地票价、地址、坐标和营业时间等权威业务字段，目前只补充远端图片和抓取时间，避免未经核验的外部事实直接进入生成链路。
2. MCP 不可用时，继续使用现有高德 Web API 或 RAG/MySQL，不影响离线演示和已有测试。
3. MCP Key 只放在 Python Agent 服务端环境变量，不进入前端，也不允许 MCP 直接写入行程数据库。
4. 行程修改仍采用“模型输出结构化操作 → Java 校验 → 用户确认 → 事务落库”的 Human-in-the-loop 流程。

默认配置为关闭。启用方式：`AMAP_MCP_ENABLED=true`、`AMAP_MCP_URL=https://mcp.amap.com/mcp`、`AMAP_MCP_KEY=服务端Key`。天气、公交和路径规划属于后续扩展方向。

---

## 三、Agent 工程化细节

### 3.1 LangGraph 工作流

项目中有两套相关但职责不同的图：`workflow.py` 的完整生成图服务 `/v1/generate`；Java 生产链路按天调用 `/v1/generate-day`，实际进入 `day_workflow.py` 的单日生成图。两者都包含确定性反思和 fallback，但节点名称与调用粒度不同。

**完整生成图 StateGraph 定义**（`workflow.py:330-346`）：

```python
def build_graph() -> StateGraph:
    graph = StateGraph(AgentState)
    graph.add_node("parse", parse_requirements)      # 任务解析：提取需求字段
    graph.add_node("search", search_pois)            # 检索候选：景点/餐饮/酒店/消费系数
    graph.add_node("generate", generate_itinerary)   # LLM 生成（含重试上限 MAX_FIX_ATTEMPTS=2）
    graph.add_node("reflect", reflect)               # 规则校验（时间/营业/饱和度）
    graph.add_node("format", format_output)           # 后处理补水（坐标/价格/图片/季节因子）
    graph.set_entry_point("parse")
    graph.add_edge("parse", "search")
    graph.add_edge("search", "generate")
    graph.add_edge("generate", "reflect")
    graph.add_conditional_edges("reflect", needs_fix, {"fix": "generate", "pass": "format"})
    graph.add_edge("format", END)
    return graph
```

**生产逐日生成图**（`day_workflow.py:108-126`）：

```text
generate → reflect
              ├─ 校验通过 → finish
              ├─ 校验失败且未达上限 → retry → generate
              └─ 达到上限 → fallback → generate → finish
```

Java 的 `ItineraryAsyncPlanner` 负责跨日状态（已使用景点、已选酒店）和落库；Python 的 `day_workflow` 负责单日生成、反思、重试与 fallback。这样既保留了 Agent 质量闭环，也能让前端按日看到结果。

**AgentState（TypedDict）**（`workflow.py:39-54`）：

```python
class AgentState(TypedDict):
    request: GenerateRequest        # 原始请求
    requirements: dict              # 解析后的需求
    candidates: list[dict]          # 候选景点
    foods: list[dict]               # 候选餐饮
    hotels: list[dict]              # 候选酒店
    consumption: dict | None        # 城市消费系数
    daily_plans: list[dict]         # 生成的每日计划
    budget_estimate: dict           # 预算估算
    result: GenerateResponse        # 最终输出
    attempts: int                   # LLM 生成尝试次数
    fix_count: int                  # 校验失败次数
    error: str | None               # 错误信息
    feedback: str                   # 校验反馈文本
    validation_issues: list[str]    # 校验问题列表
    validation_log: list[str]       # 校验日志
    degraded_reason: str | None     # fallback 或最终校验未完全通过的原因
```

**Reflect 校验规则**（`reflect.py:57-101`）：

```python
MAX_DAILY_MINUTES = 480      # 单日最大活动时长（8小时）
MAX_DAILY_ATTRACTIONS = 4    # 单日最大景点数

# 四个校验维度：
# 1. 时间重叠检测：相邻项 start_time/end_time 交叉
# 2. 营业时间覆盖：景点完整游玩区间必须落在 open_time 窗口内
# 3. 路线可达性：相邻 POI 的坐标距离必须能在空档内完成（含容错）
# 4. 单日饱和度：景点数 > 4 或总时长 > 480 分钟
```

**反馈回喂机制**（完整图 `workflow.py:143-160`；逐日图 `day_workflow.py:63-81`）：

```python
def reflect(state: AgentState) -> dict:
    issues, log = validate_plans(plans)
    return {
        "feedback": build_feedback(issues),  # "第1天时间冲突：故宫(09:00-12:00) 与天坛(11:00) 重叠"
        "fix_count": state.get("fix_count", 0) + (1 if issues else 0),
    }
```

### 3.2 RAG 混合检索

检索策略因数据类型而异，每种 POI 都有自己的最优检索路径：

**景点检索**（`tools.py:78-84`）：

```python
def search_attractions(city: str, preferences: list[str], limit: int = 30) -> list[dict]:
    # poi_store.search 内部同时执行语义召回和词法召回，再用 RRF + 业务分数重排
    hits = poi_store.search(
        _build_query(city, preferences), city=city, category="attraction",
        limit=limit, preferences=_expand(preferences),
    )
    if not hits:
        # 索引不可用或无结果时，回退 MySQL 权威查询
        hits = poi_repository.search_pois(city, category="attraction", limit=limit)
    return _sort_by_preferences(hits, preferences)[:limit]
```

**酒店检索**（`tools.py:95-103`）——优先知识库（完整可枚举）：

```python
def search_hotels(city: str, limit: int = 6) -> list[dict]:
    # 1. 优先 MySQL 全量枚举（酒店数量有限且需要完整信息做价格比较）
    rows = poi_repository.search_pois(city, category="hotel")
    # 2. 省级目的地展开到具体城市（如"浙江省" → "杭州"+"宁波"+"温州"）
    if not rows and city in DESTINATION_CITIES:
        rows = poi_repository.search_pois_by_cities(DESTINATION_CITIES[city], category="hotel")
    # 3. 向量召回兜底
    return poi_store.search(f"{city} 住宿", city=city, category="hotel")[:limit]
```

**为什么酒店不走向量检索？** 因为酒店推荐需要全量候选做价格/档次比较，向量召回可能漏掉当前档次的酒店，导致推荐偏差。

**混合检索实现**（`rag/retriever.py` + `rag/store.py`）：

- **召回**：BM25 风格词法召回 + Chroma 语义召回，使用 Reciprocal Rank Fusion（RRF）合并，并叠加偏好、评分和预算业务分数
- **默认 Embedding**：中文按单字、英文按词切分 n-gram → `zlib.crc32` 映射到 256 维哈希桶 → L2 归一化。它是离线的词法特征近似，不应表述为真正的预训练语义模型
- **可选 Embedding**：安装 `sentence-transformers` 并配置 `RAG_EMBEDDING_PROVIDER=semantic` 后，可使用本地语义模型；模型不可用时自动回退到 hashed provider
- **ChromaDB**：PersistentClient 持久化，启动时从 MySQL 全量重建索引保证一致性
- **Metadata 过滤**：`$and` 组合 city + category 过滤

### 3.3 提示词工程实践

项目中的 prompt 全部内联在各模块中，设计手法丰富：

**1. System Prompt 内嵌 JSON Schema**（`generators.py:150-168`）

```python
system_prompt = (
    "你是资深旅行规划师。只输出 JSON，不要输出任何其他文字，不要用 markdown 代码块。"
    "景点和酒店必须从候选列表中选择，不得编造。"
    + pace_clause +
    "必须严格遵守如下 JSON Schema："
    '{"type":"object","properties":{"daily_plans":...}'
)
```

首版行程生成当前主要依赖 Prompt Schema、JSON 解析和后处理校验；对话编辑的 `decide.py` 会额外传入 `response_format={"type":"json_object"}`，两条链路不要混为同一项能力。

**2. 节奏控制**（`generators.py:49-61`）

```python
def _pace_guidance(days: int) -> tuple[str, str]:
    if days <= 2:
        return "3 个景点", "短途行程可紧凑多打卡"
    if days <= 4:
        return "2-3 个景点", "中等节奏"
    if days <= 7:
        return "2 个景点", "放缓节奏，留出用餐与休息"
    return "1-2 个景点", "长途旅行以舒适慢节奏为主，避免赶场"
```

**3. 封闭动作集提示**（`decide.py:49-87`）

约 40 行的长 prompt，把任意自然语言归约到 5 种动作，详细定义每种动作的参数 schema 和禁止事项：

```python
"patches的op只能是delete、move、update、add、set_day_note。"
"delete填item_id；move填item_id、day_no和可选position；"
"酒店项目不得出现在patches中。"
"rewrite_plan 的 plan_document 必须是与'当前计划JSON'同结构的完整计划"
```

**4. 反幻觉约束**（`generators.py:150-156`）

```python
"景点和酒店必须从候选列表中选择，不得编造。"
"每个行程项的 cost 必须直接取候选数据中的 ticket_price 字段原值（单人单价）"
"严禁自行编造价格。"
```

**5. 双模型分级**（成本/延迟优化）：

- **qwen-turbo**：结构化意图识别（`decide.py:107`，temperature=0.1），低延迟
- **qwen-plus**：行程生成（`generators.py:173`，temperature=0.3），高质量

**6. 角色化 prompt**（`butler.py:14-20`）

```python
"你是一位一对一专属旅游管家，基于用户行程提供规划讲解。"
"250 字以内，语气亲切自然。"
"不要输出字面的反斜杠n，直接换行即可。"
```

### 3.4 记忆与状态管理

| 维度 | 实现 | 代码位置 |
|------|------|---------|
| **对话短期记忆** | Java 最多传最近 20 条；Python 决策层最终取最近 4 条，每条截断 800 字符 | `decide.py:89-93` |
| **跨日去重状态** | Java 侧维护 `usedNames` + `chosenHotel`，逐日回传 | `ItineraryAsyncPlanner.java:58-59` |
| **槽位记忆** | 前端 localStorage 持久化 + 合并 | `GenerateView.vue` |
| **用户偏好** | `user_preference` 表按 count 降序取 top5 | `UserPreferenceService.topPreferences` |
| **对话持久化** | `itinerary_chat_message` 表，按 itinerary_id + user_id 维度 | `ItineraryChatService.chatHistory` |
| **草稿版本控制** | SHA-256 哈希 `baseRevision`，防止旧草稿覆盖新数据 | `ItineraryChatService.chatEdit` / `requirePendingAction` |

**对话历史截断**（`decide.py:89-93`）：

```python
for item in (req.history or [])[-4:]:          # 只取最近 4 条
    role = "user" if item.get("role") == "user" else "assistant"
    content = str(item.get("content") or "")[:800]  # 每条截断 800 字符
    if content:
        messages.append({"role": role, "content": content})
```

**为什么最终是 4 条？** 完整计划和酒店候选列表已占用上下文空间，Python 决策层只保留最近 4 条（通常 2 轮对话），每条最多 800 字符；Java 侧仍保留最多 20 条历史用于持久化和组装请求。

### 3.5 对话编辑完整流程

以用户说"把第 3 天的故宫换成天坛"为例，完整流程如下：

**Step 1：前端发送**（`TripDetailView.vue:410-466`）

```typescript
const res = await chatEditItinerary(id, message, chatMsgs.value.map(m => ({
    role: m.role, content: m.content
})), abortController.signal)
```

**Step 2：Java 组装上下文**（`ItineraryChatService.chatEdit`）

```java
// 1. 加载权威状态
List<Map<String, Object>> persistedHistory = chatHistory(userId, itineraryId);
String baseRevision = sha256(objectMapper.writeValueAsString(currentPlans));

// 2. 构建对话体（含 city/days/persons/budget/current_total/plans/history/message）
chatBody.put("plans", currentPlans);
chatBody.put("history", effectiveHistory.size() <= 20 ? effectiveHistory
        : effectiveHistory.subList(effectiveHistory.size() - 20, effectiveHistory.size()));
chatBody.put("baseRevision", baseRevision);

// 3. 调用 Python Agent
JsonNode node = agentService.chatTurn(chatBody);
```

**Step 3：Python Agent 决策**（`decide.py:126-267`）

```python
# 1. LLM 返回封闭动作决策
decision = _decide_plan_change(req, hotels)
# {"mode": "plan_update", "patches": [{"op": "update", "item_id": 123, "fields": {"poi_name": "天坛"}}]}

# 2. 确定性代码执行补丁
plans = _apply_plan_update(decision, req)

# 3. 防御性校验
plans = _dedupe_plans(plans)           # 去重
_hotel_signature(plans)                # 酒店签名校验
_substantive_plan_signature(plans)     # 实质变更检查
_plan_conflict(plans)                  # 时间冲突检查
```

**Step 4：Java 落库**（`ItineraryChatService.chatEdit`）

```java
// ItineraryChatService 保存用户消息和 AI 消息到数据库
saveChatMessage(userId, itineraryId, "user", message, ...);
saveChatMessage(userId, itineraryId, "ai", reply, plans, hotelOptions, changed);

// 返回草稿（不落库！）
return Map.of("reply", reply, "plans", plans, "changed", changed,
              "baseRevision", baseRevision, ...);
```

**Step 5：用户确认应用**（`ItineraryPlanApplyService.applyPlans`）

```java
// 1. 校验版本一致性
if (!baseRevision.equals(currentRevision)) {
    throw new BizException(409, "草稿版本已过期");
}

// 2. 从 AI 消息的 plansJson 读取权威数据
List<Map<String, Object>> plans = objectMapper.readValue(message.getPlansJson(), ...);

// 3. 事务内逐项落库
@Transactional
public ItineraryVO applyPlans(...) {
    // 逐日逐项更新/插入/删除 ItineraryItem
    // 删除超出新天数的日期
    // 更新主表天数/日期/标题
    // 重算预算
}
```

**面试价值**：这套"模型出意图、代码做执行、用户确认才落库"的三层架构，是 Human-in-the-loop 的经典实现，有效防止了 LLM 的不可靠输出直接污染数据库。

---

## 四、数据库设计

### 4.1 核心表结构

```sql
-- 用户表
sys_user (id, username, password, nickname, phone, status, created_at)

-- 行程主表
itinerary_main (
    id, user_id, title, city, start_date, end_date,
    days, persons, budget,                   -- 基本参数
    preferences, hotel_tier,                 -- 偏好（逗号分隔）
    status,                                  -- 1=生成中 2=已生成 3=生成失败
    plan_note,                               -- AI管家规划讲解
    created_at, updated_at, deleted
)

-- 日行程表
itinerary_day (
    id, itinerary_id, day_no, travel_date, city, note
)

-- 行程项表（景点/餐饮/酒店/交通）
itinerary_item (
    id, day_id, itinerary_id,
    item_type,                               -- attraction/food/hotel/transport
    poi_name, poi_id, address,
    latitude, longitude,                     -- 坐标（地图展示）
    start_time, end_time, duration_min,      -- 时间轴
    cost, tag, remark, intro,                -- 价格/标签/备注/介绍
    sort_no,                                 -- 拖拽排序
    created_at, updated_at, deleted
)

-- 对话记忆表
itinerary_chat_message (
    id, itinerary_id, user_id,
    role,                                    -- user/ai
    content,
    plans_json,                              -- 草稿方案 JSON
    hotel_options_json,                      -- 酒店候选 JSON
    changed,                                 -- 是否有实质变更
    created_at
)

-- 预算明细表
budget_detail (
    id, itinerary_id, day_id,
    category,                                -- 门票/餐饮/酒店/交通
    amount, item_count, remark
)

-- 用户偏好统计表
user_preference (
    id, user_id, pref_label, count,          -- 按 count 降序取 top5
    created_at, updated_at
)

-- 知识库表
poi_knowledge (
    id, city, name, category, address,
    latitude, longitude,
    ticket_price, duration_min, open_time,
    tags, rating, description
)

-- 酒店房型表
hotel_room_type (
    id, poi_id, room_name, base_price,
    capacity, bed_type, breakfast, description, is_default
)

-- 城市消费系数表
city_consumption (
    id, city, level,
    meal_price, transport_price, hotel_price
)

-- 异步导出任务表
export_task (
    id, itinerary_id, user_id,
    task_type, status, file_path, error_msg,
    created_at, finished_at, deleted
)
```

### 4.2 ER 关系图

```
sys_user (用户)
  ├── itinerary_main (行程主表)
  │     ├── itinerary_day (日行程)
  │     │     └── itinerary_item (行程项)
  │     ├── itinerary_chat_message (对话记忆)
  │     ├── budget_detail (预算明细)
  │     └── export_task (导出任务)
  └── user_preference (用户偏好统计)

poi_knowledge (景点知识库)
  └── hotel_room_type (酒店房型)

city_consumption (城市消费系数)
```

### 4.3 关键设计决策

| 决策 | 理由 |
|------|------|
| `itinerary_item.sort_no` | 支持拖拽排序，前端 vuedraggable 改变顺序后批量更新 |
| `itinerary_chat_message.plans_json` | 对话中的草稿方案以 JSON 存储，支持"应用到行程"时读取权威数据 |
| `itinerary_main.status` | 1=生成中 / 2=已生成 / 3=生成失败，异步生成过程中前端可通过 status 轮询 |
| 逻辑删除 `deleted` | 行程主表、行程日、行程项、预算和导出任务等可变业务数据使用逻辑删除，避免直接物理删除；当前未提供通用恢复接口 |
| `user_preference` 独立表 | 按用户维度统计偏好选择次数，支持跨行程的偏好推荐 |
| `baseRevision` SHA-256 | 防止旧草稿覆盖新数据，对话编辑的版本控制 |

---

## 五、系统配置与运维

### 5.1 Java 端关键配置（`application.yml`）

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:travel_assistant}?useUnicode=true&characterEncoding=utf8mb4
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6380}
      password: ${REDIS_PASSWORD:}

app:
  jwt:
    secret: ${JWT_SECRET}
    expire-hours: 24       # 24小时
  agent:
    base-url: ${AGENT_SERVICE_URL:http://localhost:8000}
    internal-token: ${AGENT_INTERNAL_TOKEN:}
  amap:
    web-key: ${AMAP_WEB_KEY:}
  unsplash:
    access-key: ${UNSPLASH_ACCESS_KEY}
  export:
    dir: ${EXPORT_DIR:data/export}
```

### 5.2 Python 端依赖（`pyproject.toml`）

```toml
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi==0.111.0",
    "uvicorn[standard]==0.30.1",
    "pydantic==2.8.2",
    "httpx==0.27.0",
    "mcp>=1.10,<2",
    "python-dotenv==1.0.1",
    "langgraph==0.2.44",
    "langchain-core==0.3.15",
    "pymysql==1.2.0",
    "chromadb==1.5.9",
]
```

Python 项目要求 3.12；`sentence-transformers` 不属于默认依赖，作为可选 `rag` extra 用于语义 Embedding，默认使用零外部模型依赖的 hashed provider。

### 5.3 前端依赖（`package.json`）

```json
{
  "dependencies": {
    "vue": "^3.4.27",
    "vue-router": "^4.3.2",
    "pinia": "^2.1.7",
    "element-plus": "^2.7.6",
    "axios": "^1.7.2",
    "echarts": "^6.1.0",
    "@amap/amap-jsapi-loader": "^1.0.1",
    "vuedraggable": "^4.1.0",
    "@iconify/vue": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "@vitejs/plugin-vue": "^5.0.4",
    "typescript": "~5.4.5",
    "vite": "^5.2.11",
    "vue-tsc": "^2.0.21"
  }
}
```

### 5.4 当前验证结果与评测口径

以下结果来自当前工作树的本地验证。Python/Java/前端基础验证于 2026-09-01 完成；Agent 离线评测、真实 LLM 评测和服务冒烟数据沿用 2026-08-29 的报告。真实 LLM 结果会受到模型、Prompt、知识库快照和网络状态影响，不能当作永久 SLA：

| 验证项 | 结果 | 说明 |
|------|------|------|
| Python 单测 | 72 passed | `pytest --ignore=tests/api -q`，离线测试，不含需要独立服务的 API 测试 |
| Java 单测 | 8 passed | `mvn -q test`，预算引擎 JUnit/Mockito 用例 |
| 前端类型检查/生产构建 | 通过 | `vue-tsc --noEmit` + Vite build；构建有大 chunk 警告 |
| Agent 离线评测 | 6 个固定案例；权威 POI、字段引用、冲突、路线违规、重复和预算偏差均达到 0/100% 目标 | 使用 fixture + 确定性 fallback，不代表 LLM 生成质量 |
| 真实 LLM 双跑 | 6 个案例、12 次运行；一致性率 100%，failed 0 | 12 次均为 `degraded`，12 次触发 fallback |
| 真实 LLM 消耗 | 60 次调用，434,521 tokens | qwen-plus，temperature=0.3；详见 `tests/agent_eval/report/llm_report.json` |
| 服务冒烟 | Java/Agent 健康检查 HTTP 200，受保护接口未登录返回 401 | 本地服务验证 |

评测中的“成功”分为两层：业务结果可用不等于 LLM 直出成功。本次真实 LLM 评测的一致性较好，但由于模型输出经过校验后全部走了确定性 fallback，最终状态应诚实标为 `degraded`，不能写成“LLM 成功率 100%”。

---

## 六、面试深度问题预判与回答要点

### Q1: 为什么不用 LangChain Agent / AutoGPT？

**答**：旅行规划是**强正确性**场景——时间冲突、地理不合理、价格虚高都会直接损害用户体验。让 Agent 自主决定工具调用顺序会导致不可控的规划路径。我们选择"LLM as planner, code as executor"范式：LLM 只做结构化决策（5 种封闭动作），所有执行由确定性代码完成并多重校验。这在可控性、可调试性、成本可控性上都优于 fully autonomous agent。

**追问：那你这个算 Agent 吗？**

这是一个**受约束的 Agent workflow**——有 LangGraph 编排、有 RAG 增强、有反思循环，并通过 MCP Client 做工具发现与外部事实查询；但没有把数据库写入权交给模型。工具调用受到语义映射、只读白名单、超时和降级策略约束，这是强正确性场景下对自主性与可控性的平衡。

### Q2: Reflect 循环的实际效果如何？

**答**：`reflect.py` 做四类确定性校验——时间重叠、营业时间完整覆盖、基于坐标的路线可达性、单日饱和度（上限 480 分钟 / 4 景点）。路线耗时使用道路距离折算，并额外加入 10 分钟固定缓冲和 25% 比例容错；它是规划阶段的保守门槛，不冒充实时导航。离线 fixture 与真实 LLM 评测结果分别见 `tests/agent_eval/report/report.md` 和 `tests/agent_eval/report/llm_report.json`。

### Q2.1: 如何证明 Agent 真的执行了工作流？

**答**：每次生成都有脱敏 `run_id` 和事件轨迹，能看到节点、工具、LLM 请求、条件路由、重试和 fallback；可通过响应头拿到 `run_id`，再用 `/v1/runs/{run_id}` 查询最近 100 条轨迹，同时 `/v1/metrics` 聚合成功、降级、失败、延迟、token、MCP 失败率。轨迹不保存完整 Prompt、用户原文和密钥。完整生成图离线评测会校验 `parse/search/generate/reflect/format`；生产逐日链路则记录 `day.generate/day.reflect/retry/fallback` 等事件。失败案例回放验证未知 POI、路线不足和营业时间越界都会被拦截。

**追问：为什么不直接在 prompt 里约束时间不重叠？**

已经约束了（`generators.py:158`："每天行程需避免相邻项时间重叠"），但 LLM 仍然会犯错——它不擅长精确的时间计算。所以需要 reflect 层做确定性校验 + 反馈回喂，让模型在第二轮修正。

### Q3: 如何处理 LLM 幻觉？

**答**：四层防幻觉机制：
1. **Prompt 约束**：明确要求"景点必须从候选列表选择，不得编造"
2. **结构约束**：首版生成使用 Prompt 内嵌 Schema；对话编辑额外使用 `response_format=json_object`
3. **后处理校验**：`format_output` 阶段用候选列表做坐标/票价/时长补水，LLM 输出的值被权威数据覆盖
4. **确定性兜底**：LLM 失败时完全不依赖 LLM，用最近邻算法 + 时间槽模板生成

### Q4: 逐日生成的跨日一致性如何保证？

**答**：Java 侧 `ItineraryAsyncPlanner` 维护两个跨日状态：`usedNames`（已选过的景点名集合）和 `chosenHotel`（已选酒店名）。每天生成时把这两个集合传给 Python，Python 在 `day_stream.py:191-199` 过滤已使用 POI并把酒店约束回喂给生成器。酒店通过 `chosenHotel` 尽量确保整条行程住同一家酒店。

**追问：如果某天 LLM 失败了怎么办？**

该天走 fallback 兜底（确定性算法），但 `usedNames` 仍然更新，不会影响后续天数的一致性。如果整个 `planDays` 异步任务失败，`fail()` 方法会将 status 设为 3（生成失败）。

### Q5: 对话编辑的版本控制如何实现？

**答**：`chatEdit` 方法在每次请求时计算当前 plans 的 SHA-256 哈希作为 `baseRevision`，传给 Python Agent。Agent 返回的草稿携带该 revision。用户点击"应用"时，`applyPlans` 校验 revision 一致性——如果期间有其他修改导致 plans 变化，revision 不匹配则拒绝应用，防止旧草稿覆盖新数据。

### Q6: 性能优化做了哪些？

**答**：
- **Redis 缓存**：行程详情使用 `@Cacheable`，编辑后 `@CacheEvict` 清除
- **图片获取链路**：进程内 dict 缓存；数据源按 Wikipedia → Unsplash → 高德 POI 图片顺序降级
- **联网查询限额**：`MAX_LIVE_QUERIES=3`，防止 LLM 联网搜索成本失控
- **对话历史截断**：Java 最多传 20 条，Python 决策最终取 4 条，每条最多 800 字符
- **前端路由懒加载**：`() => import(...)`，首屏只加载必要代码
- **渐进式生成**：逐日落库 + 清缓存，前端轮询可见新数据

### Q7: 开放模式（未知城市）怎么处理？

**答**：`day_stream.py:201-205`：当知识库无该城市候选时，进入开放模式——LLM 凭自身知识安排一天行程（`_llm_open_day`），输出的景点尝试由高德 MCP 查询落地坐标与地址；MCP 未启用或失败时兼容高德 Web API。开放模式的酒店由 LLM 自行选择，价格标注“供参考”；这条路径依赖外部服务，不能与本地权威候选约束路径等量齐观。

### Q8: 如果让你重新设计，会改什么？

**答**（展示自我反思能力）：

1. **语义检索升级**：当前默认使用 crc32 哈希 n-gram 做离线词法特征；虽然已预留 `sentence-transformers` provider，但还应补充真实语义模型的离线评测和资源治理
2. **消息队列**：当前用 Spring `@Async` 内存线程跑异步生成，服务重启会导致"生成中"状态的行程永久卡死。应该引入 RabbitMQ/Kafka 做任务持久化
3. **可观测性**：当前是单进程内存指标和最近 100 条脱敏 Trace，生产环境需要接入 Prometheus/OpenTelemetry + Grafana，并增加跨实例 Trace 存储与采样策略
4. **记忆增强**：当前对话记忆是无状态的滑动窗口，应该引入 summary memory（定期压缩历史）和 user profile（长期偏好画像）
5. **流式响应**：当前是请求-等待-响应模式，应该用 SSE/WebSocket 实现真正的流式输出，用户体验更好
6. **MCP 能力扩展**：当前已接入高德官方 MCP 的只读 POI 查询适配，默认关闭且失败可降级；下一步可将天气、公交路线、地理编码和距离测量等能力接入更完整的外部事实链路。

---

## 七、项目亮点总结

> 这是一个工程化程度较高的 AI 旅行规划系统，核心亮点是：
>
> 1. **"LLM 当规划者、代码当执行者"的分离范式**——LLM 只做结构化决策，所有执行由确定性代码完成并多重校验
> 2. **多层降级容错体系**——五层防御纵深（宽松解析 → self-repair → 反馈回喂 → 确定性兜底 → 签名校验）
> 3. **异步渐进式生成**——逐日增量生成 + 跨日状态管理，让前端可以先展示已完成日期
> 4. **对话编辑的版本控制与 Human-in-the-loop**——草稿确认机制、SHA-256 版本校验、酒店签名防篡改
> 5. **封闭动作集提示词设计**——把任意自然语言归约到 5 种有限决策空间
> 6. **RAG 混合检索**——BM25 风格词法召回 + Chroma 语义召回，RRF 融合后再做偏好/评分/预算重排；酒店保留全量可枚举候选
> 7. **完整的降级链路**——在存在权威候选数据时，LLM 不可用可由确定性算法兜底；开放城市仍依赖 LLM/外部地图能力
>
> 覆盖了从 LLM 结构化输出、RAG 检索、封闭动作集提示词、异步编排到事务落库的完整链路。
